# Homelab Configuration

A collection of Docker Compose and other files used to run my homelab

### Instructions

The media stack requires some environment variables to work. In `media/`, change `.env.example` to `.env` and add the relevent API key and secret token in place of the variables.
